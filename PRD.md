# 设计优化：吃啥决定器 v2

## 核心理念

让它看起来不像一个"工具"，像 **一个漂亮的物件**。

## 具体改动

### 1. 配色体系
- **背景**：深色模式 `#1d1d1f`（类 Apple 深空灰），非白色
- **转盘色板**：选一个有质感的暖色调色板，不要彩虹色
  - 主色调：#FF6B35（橙）、#F7C948（金）、#E85D75（粉红）、#A78BFA（紫）、#4ECDC4（青）
  - 饱和度控制，不要荧光色
- **卡片/文字**：白色/浅灰，有层次

### 2. 转盘视觉效果
- 段与段之间用细白线分隔，线条带 0.3 透明度
- 外圈加一圈细渐变描边（模拟金属圈效果）
- 每段不要纯色填充，用 subtle 渐变（同一色相深浅过渡）
- 文字加白色阴影增加可读性
- 转盘在静止时有一条 micro-glow（外发光，`#FF6B35` 微弱光晕）

### 3. 指针
- 不要大三角
- 改用一个精巧的 diamond / 菱形指针，或者一个极简的小三角形
- 颜色用主色调，加 drop-shadow

### 4. 中间按钮
- 不要"转！"那么大个文字
- 改用简化设计：一个小圆点 + 箭头 icon，或一个精致的小圆按钮
- 静止时：显示 ▶️ play icon
- 旋转时：显示 ⋯
- 背景磨砂玻璃效果（backdrop-filter: blur）

### 5. 动画升级
- 旋转时转盘带 **motion blur** 效果（用 canvas 实现或旋转帧模糊）
- 停止时加一个微小的 **bounce back** 弹性效果
- 结果弹出用 **spring animation**（不是简单 popIn，带 overshoot）
- 彩纸换成更精致的效果：更少但更大的粒子，金色银色为主

### 6. 结果页
- 全屏覆盖（不是卡片）
- 背景模糊
- 食物用超大字体：60-80px
- 下面配个 emoji
- 按钮改成纯 icon 风格（⟳ 再来 / 📤 分享）

### 7. 选项管理
- 标签圆角更柔和（pill shape）
- 添加框整合进界面，不要突兀的输入框
- 预设按钮用更加 subtle 的标签式设计

### 8. 微交互
- 添加选项时标签飞入动画（spring）
- 删除选项时标签飞出动画
- 点击 spin 按钮的按压反馈
- 转盘在 idle 状态有非常微弱的 pulse 光晕（2s 循环）

## 参考风格

- Apple Design Awards 级别的精致感
- 一个词概括：**refined**

## 实现要求

更新 `/Users/tanger/.openclaw/workspace/pm/projects/eat-decider/index.html`
保持 PWA 功能不变，只改视觉和动画。

完成后 git commit，通知 Ben 验收。

Notification route:
- channel: telegram
- target: '8432128725'
- message: 'Cole: 吃啥决定器 v2 设计翻新完成，可以验收了！🎨'